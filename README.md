# adult-dataset-preprocessing
This repository demonstrates data preprocessing on the Adult Income dataset using Python. It covers identification of numerical and categorical features, label encoding, one-hot encoding, feature scaling with StandardScaler, and saving a clean dataset ready for machine learning models.
